<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mindmie.mindappversion1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".NameQActivity" />
        <activity android:name=".ShowInfoActivity" />
        <activity android:name=".AgeQActivity" />
        <activity android:name=".GenderQActivity" />
        <activity android:name=".WeightQActivity" />
        <activity android:name=".HeightQActivity" />
        <activity android:name=".ActivityFactorQActivity"></activity>
    </application>

</manifest>